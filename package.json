{
  "name": "task",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npx playwright test",
    "test:login": "npx playwright test tests/LoginPageTest.spec.ts",
    "test:e2e": "npx playwright test --grep '@end-to-end'",
    "test:smoke": "npx playwright test --grep '@smoke'",
    "test:critical": "npx playwright test --grep '@critical'",
    "test:ui": "npx playwright test --ui",
    "test:headed": "npx playwright test --headed",
    "test:debug": "npx playwright test --debug",
    "test:report": "npx playwright show-report",
    "test:install": "npx playwright install --with-deps",
    "lint": "eslint . --ext .ts",
    "lint:fix": "eslint . --ext .ts --fix",
    "format": "prettier --write \"**/*.{ts,js,json,md}\"",
    "metrics": "node -e \"import('./utilities/TestMetrics.ts').then(m => m.TestMetricsCollector.generateReport())\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@types/node": "^24.3.0",
    "allure-playwright": "^3.3.3"
  },
  "dependencies": {
    "csv-parse": "^6.1.0"
  }
}
